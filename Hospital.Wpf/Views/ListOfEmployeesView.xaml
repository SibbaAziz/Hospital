<UserControl x:Class="Hospital.Wpf.Views.ListOfEmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hospital.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Content="{StaticResource AppbarRefresh}" Style="{StaticResource BackgroudButton}"  Height="30" VerticalAlignment="Top" Command="{Binding RefreshCommand}"></Button>
            <DataGrid ItemsSource="{Binding Employees}" Grid.Column="1"></DataGrid>
        </Grid>
    </Border>
</UserControl>
